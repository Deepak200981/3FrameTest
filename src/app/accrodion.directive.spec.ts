import { AccrodionDirective } from './accrodion.directive';

describe('AccrodionDirective', () => {
  it('should create an instance', () => {
    const directive = new AccrodionDirective();
    expect(directive).toBeTruthy();
  });
});
